function isStrong(pass){
    if(pass.length < 8){
        return false;
    }
    let up =0;
    let lc = 0;
    let sp =0;
    let dig = 0;

    for(let i=0;i<pass.length;i++){
        let ind = pass.charAt(i);
        if(ind == '@' || ind == '$'){
            sp++;
            continue;
        }else if{

        }
    }
}